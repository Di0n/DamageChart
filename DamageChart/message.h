#pragma once
#include "..\inc\natives.h"
#include <string>
#include <vector>

using std::string;


void ShowSubtitle(const string& text, const int duration);
void ShowNotification(const string& text);